#ignore all parquet data files - millions of lines long
*.parquet

__pycache__/
*.py[cod]
.cache/

.venv

# Use this command for every large file you want to untrack
git rm --cached <file_name>.parquet
venv

guides/*

# Local roadmap / personal next steps (not shared)
future-updates.local.md

# TLC download manifest (checksums; parquet already ignored)
taxi data/.tlc_manifest.json