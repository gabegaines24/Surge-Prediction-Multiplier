# Data processing
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
fastparquet>=2024.11.0
PyYAML>=6.0.0

# Dask for large data processing
dask[complete]>=2024.1.0
distributed>=2024.1.0

# Weather API (for weather_service.py)
requests-cache>=1.0.0
retry-requests>=2.0.0
openmeteo-requests>=1.0.0

# Machine Learning
scikit-learn>=1.3.0
xgboost>=2.0.0
joblib>=1.3.0

# API (FastAPI)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
cachetools>=5.3.0

# Optional experiment tracking (enable in backend/config.yaml or SURGE_MLFLOW_ENABLED=1)
mlflow>=2.10.0

# Progress visualization
tqdm>=4.65.0
